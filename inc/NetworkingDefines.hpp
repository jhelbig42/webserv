#pragma once

#define PORT "3499"      // will later be determined by config file (argv[1]);
#define MAX_REQUEST 1024 // will later be determined by config file
#define BACKLOG 10
#define BYTES_PER_CHUNK 256
